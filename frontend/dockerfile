# Use a lightweight Nginx image
FROM nginx:1.25-alpine

# Copy your HTML, CSS, and JS files into the default Nginx web root
COPY . /usr/share/nginx/html

# Nginx container exposes port 80 by default
EXPOSE 80