include:
  - repo: gh:bluetext-io/bluetext
  - modules/server
  - modules/temporal
  - modules/postgres
tools:
  default:
    run:
      - tool: "server"
      - tool: "temporal"
      - tool: "temporal-ui"
      - tool: "postgres"
